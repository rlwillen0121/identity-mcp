# Copy into ~/.codex/config.toml (merge) or a project .codex/config.toml.
# Contributor-only Codex profile. It intentionally contains no identity MCP
# servers. Codex does not provide a per-agent MCP boundary, so never merge the
# operator profile into this file or start a contributor process with IdP
# credentials in its environment.
#
# Use examples/codex.operator.config.toml only in a separate operator process.
