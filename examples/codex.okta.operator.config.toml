# Okta-only operator profile. Load only in a separate, clean operator process.

[mcp_servers.okta]
command = "okta-mcp"
startup_timeout_sec = 15

[mcp_servers.okta.env]
OKTA_ORG_URL = "${OKTA_ORG_URL}"
OKTA_API_TOKEN = "${OKTA_API_TOKEN}"
